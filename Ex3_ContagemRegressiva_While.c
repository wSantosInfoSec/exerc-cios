#include <stdio.h>
#include <stdlib.h>
int main (){
int nUser;

printf ("Escolha um número para a contagem regressiva:" );

scanf ("%d", &nUser);
	
	
	while (nUser>=0) {
		printf ("%d\n", nUser);
	nUser--;}
}
RETURN 0
